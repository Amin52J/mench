### Phase 5.1 — PWA shell ⬜ `[composer]`

**Gate:** Lighthouse installable PWA; app opens standalone on mobile emulator.

**Goal:** Installable mobile experience per `decisions.mdc` O10.

- [ ] Web app manifest (name Mench, icons, theme_color, display standalone)
- [ ] Service worker caches app shell; network-first for API/WebSocket
- [ ] Offline page when online play unavailable
- [ ] Viewport + safe-area CSS; touch-action on board

**Notes:** Placeholder icons OK; replace before public launch.
