### Phase 5.2 — Production deploy ⬜ `[composer]`

**Gate:** Production URL smoke test passes `roadmap.mdc` wave 5 acceptance.

**Goal:** Wrangler production deploy for SPA + Worker.

- [ ] `pnpm deploy` script wiring `wrangler deploy` + client build
- [ ] Production env in `wrangler.toml` (routes, DO bindings)
- [ ] README: deploy steps, required Cloudflare account setup
- [ ] Smoke checklist: create room, join from phone, play one turn

**Notes:** Custom domain optional follow-up.
