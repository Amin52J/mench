/** Must match `--piece-step-duration` in `tokens.css` (UI-only). */
export const PIECE_STEP_MS = 260;

/** Must match `--piece-drag-duration` in `tokens.css` (capture return). */
export const CAPTURE_DRAG_MS = 580;

export const CAPTURE_FLASH_MS = 420;
